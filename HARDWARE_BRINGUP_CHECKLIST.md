# 新 STM32 板安全上电与分模块接入

## 对旧板现象的判断

旧板在只跑 USB 监视时 STM32 已明显升温，随后杜邦线接触后冒烟，最终 MCU
封装碳化且 ST-Link 同时发热。无法再从烧毁后的板子唯一确定原因，但现象与
以下一种或多种情况相符：

- 带电杜邦线瞬间把 3.3 V、5 V、GND 或 GPIO 短接；
- 5 V 进入非 5 V 容忍引脚或 3.3 V 电源轨；
- USB、开发板电源和 ST-Link 电源同时连接，形成电源争用或反向灌电；
- ST-Link 的 `VTref/T_VCC` 被误当成开发板主电源；
- 外设先上电，通过信号引脚给未上电的 STM32 反向供电；
- 杜邦线、模块接线或旧 ST-Link 已经损坏。

40～50°C 的估计值本身未必足以烧毁芯片，但“温度持续快速上升”已经是异常
信号。封装碳化说明最终发生了远高于正常工作的局部过流或过压。

## 禁止事项

- 不再给旧板通电。
- 发热过的 ST-Link 未经独立检查，不连接新板。
- 不复用来历不明、可能插错过的整束杜邦线。
- 不在带电状态下拿松散杜邦线探碰排针。
- 不同时使用 USB、ST-Link 3.3 V 和外部电源给目标板供电。
- 未查清 ST-Link 型号说明前，不把 `VTref/T_VCC` 当电源输出。

## 第一次上电

1. 新板不连接任何传感器、Flash、继电器、USB 数据线或旧 ST-Link。
2. 断电测量 `3V3-GND`、`5V-GND` 电阻；若稳定接近短路，停止上电。
3. 根据开发板原理图选择唯一的正确电源入口：
   - 走板载稳压器时，从指定 5 V/VIN 输入；
   - 只有确认允许直接供电时，才向 3.3 V 电源轨输入 3.3 V。
4. 优先使用带限流和电流显示的电源。裸板可从较保守限流开始，确认没有
   异常后再逐步提高；具体正常电流以该开发板原理图和裸板实测为准。
5. 上电后立即测量 3.3 V 电源轨，并观察输入电流和 MCU 温升。
6. 若电流持续顶住限流、3.3 V 明显异常或 MCU 数秒内快速升温，立即断电。

## 调试器接入

1. 先确认 ST-Link 自身不发热，并尽量使用已知正常的调试器。
2. 目标板使用自己的唯一电源。
3. 断电连接 `GND、SWDIO、SWCLK、NRST、VTref`。
4. `VTref` 默认只作为目标电压检测；只有对应型号手册明确允许时才由调试器
   给目标板供电。
5. 先烧录最小 LED/空循环程序，稳定运行十分钟后再烧录完整工程。

## 外设逐个接入

每次断电，只增加一个模块，然后重新检查电流、3.3 V 和温升：

1. W25Q64；
2. 单个 I2C 传感器；
3. HX711；
4. 其他输入模块；
5. USB；
6. 最后才连接继电器、风扇、制冷和其他大电流负载。

每个信号必须确认：

- 共地可靠；
- STM32 引脚电压不超过允许范围；
- 外设不会在 STM32 断电时通过 GPIO 反向供电；
- 5 V 输出已使用分压、限流或电平转换；
- 感性负载有续流和电源隔离，负载电流不经过 MCU/调试器供电轨。

## 完整固件验收顺序

1. 裸板启动与时钟；
2. USB 串口；
3. `preflight` 的 Cube.AI 全零输入自检；
4. 两片 Flash；
5. 每个传感器单独上线；
6. 运行 10 分钟观察电流、温升和 FreeRTOS heap；
7. 才开始 1 Hz 历史采集和样本标注。

